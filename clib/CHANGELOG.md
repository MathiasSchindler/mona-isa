# clib Changelog

## 0.8.0
- Added minimal heap functions: `malloc`, `free`, `calloc`, `realloc`.
- Added libc wrappers for `putchar`, `puts`, and `exit` with optional prefer-libc path.
- Added `write`/`read` stubs for file-descriptor style I/O.
